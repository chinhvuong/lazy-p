{
  "name": "@lazy-p/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "happy-dom": "^14.12.3",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
